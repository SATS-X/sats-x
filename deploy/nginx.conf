server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: mọi route không khớp file tĩnh đều trả index.html cho React Router xử lý.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cùng-origin với frontend nên trình duyệt không cần CORS, cookie refresh
    # token (httpOnly, SameSite) cũng hoạt động đúng vì cùng site.
    location /api/ {
        proxy_pass http://app:4000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|woff2?|ttf)$ {
        expires 7d;
        add_header Cache-Control "public, max-age=604800, immutable";
    }
}
